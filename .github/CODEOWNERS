# Every path needs an owner that GitHub can actually resolve.
#
# A handle that does not resolve makes the whole rule silently inert: GitHub does
# not warn, it just never requests the review. PLAN.md lists @pratik and @savidis
# for submissions/** and the docs, but those are placeholders, not real handles.
# They are deliberately omitted until confirmed, and added at Phase 12 transfer
# alongside the required-review rule.

* @JiwaniZakir

# TODO(phase-12): once real handles are confirmed, restore these AND enable
# require_code_owner_reviews with required_approving_review_count: 1. Until both
# are on, CODEOWNERS is advisory only.
#
#   /submissions/                   @pratik-handle
#   /docs/CARD.md                   @savidis-handle
#   /docs/SUBMISSION.md             @savidis-handle
#
# These two need a human on every change, because they are what stops arbitrary
# code execution reaching the runner:
#   /.github/                       @JiwaniZakir @second-reviewer
#   /tools/checks/no_unpickling.py  @JiwaniZakir @second-reviewer
#   /tests/test_security.py         @JiwaniZakir @second-reviewer
